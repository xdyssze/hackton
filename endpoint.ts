

import express from "express";
//import Processing from "processing";

const router = express.Router();

router.post("/", (req, res) => {
    // const { body } = req;
    // const { data } = body;
    // //const processing = new Processing();

    // //const result = processing.process(data);

    // //res.json(result);
});

export default router;